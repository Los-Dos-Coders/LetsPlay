version: '3'

services:
  frontend:
    build: ./letsplay
    ports:
      - "80:80"
    container_name: frontend-docker
    restart: always
    links:
      - backend
  
  backend:
    build: ./letsplay-server
    ports:
      - "8000:8000"
    container_name: backend-docker
    restart: always